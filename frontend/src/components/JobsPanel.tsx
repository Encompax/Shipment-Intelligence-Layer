import React from "react";

const JobsPanel: React.FC = () => (
  <div className="panel-placeholder">
    <p>Control, implementation, and tracking of data movement projects and pipeline jobs.</p>
    <p>Upcoming: job status timeline, error replay, and run history.</p>
    <span className="placeholder-pill">Coming in Phase 2</span>
  </div>
);

export default JobsPanel;
