import React from "react";

const PlanningPanel: React.FC = () => (
  <div className="panel-placeholder">
    <p>Demand, capacity, and materials planning in a single hub.</p>
    <p>Upcoming: forecast accuracy, coverage days, and rough-cut capacity views.</p>
    <span className="placeholder-pill">Coming in Phase 2</span>
  </div>
);

export default PlanningPanel;
